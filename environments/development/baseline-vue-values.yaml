service:
  type: ClusterIP
  port: 80
ingress:
  enabled: true
  className: "traefik"
  annotations:
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: vue.131.153.227.137.sslip.io
      paths:
        - path: /
          pathType: Prefix
  tls: []